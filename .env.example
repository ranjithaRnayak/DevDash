# DevDash Environment Variables
# Copy this file to .env and fill in your values
#
# IMPORTANT: All sensitive tokens (PAT, API keys) should be stored in the
# backend appsettings.json or environment variables, NOT in the frontend.
# The frontend only needs the API URL and OAuth client IDs (which are safe to expose).

# ============================================
# Backend API Configuration
# ============================================
# URL to the DevDash backend API (handles all authenticated API calls)
VITE_API_URL=http://localhost:5000/api

# ============================================
# Microsoft Entra ID Configuration (OAuth - Client IDs are safe to expose)
# ============================================
VITE_ENTRA_CLIENT_ID=your_entra_client_id
VITE_ENTRA_TENANT_ID=your_entra_tenant_id
VITE_ENTRA_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_ENTRA_AUTHORIZE_URL=https://login.microsoftonline.com
VITE_ENTRA_SCOPES=openid,profile,email,User.Read

# ============================================
# GitHub OAuth Configuration (Client IDs are safe to expose)
# For GitHub Enterprise, update the URLs to your instance
# ============================================
VITE_GITHUB_OAUTH_ENABLED=true
VITE_GITHUB_CLIENT_ID=your_github_oauth_app_client_id
VITE_GITHUB_AUTHORIZE_URL=https://github.yourcompany.com/login/oauth/authorize
VITE_GITHUB_REDIRECT_URI=http://localhost:5173/auth/github/callback
VITE_GITHUB_SCOPES=read:user,repo

# ============================================
# SonarQube Configuration (Proxied through Vite in dev)
# Token is optional here - configure in backend for production
# ============================================
VITE_SONARCUBE_URL=https://sonarqube.yourcompany.com
VITE_SONARCUBE_REPO=your-project-key,another-project-key

# ============================================
# App Environment
# ============================================
VITE_APP_ENV=dev

# ============================================
# BACKEND CONFIGURATION (appsettings.json)
# ============================================
# The following sensitive values should be configured in the backend:
#
# appsettings.json or environment variables:
#   - AzureDevOps:PAT (Azure DevOps Personal Access Token)
#   - AzureDevOps:OrganizationUrl
#   - AzureDevOps:Project
#   - AzureDevOps:Repos
#   - GitHub:PAT (GitHub Personal Access Token)
#   - GitHub:ApiUrl
#   - GitHub:Owner
#   - GitHub:Repo
#   - MicrosoftGraph:ClientId
#   - MicrosoftGraph:ClientSecret
#   - MicrosoftGraph:TenantId
#   - SonarQube:Token
#   - SonarQube:Url
